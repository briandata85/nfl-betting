name: Backtest Spread and Total Models

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/backtest_spread_total.py'
      - 'tests/test_backtest_spread_total.py'
      - '.github/workflows/backtest-spread-total.yml'

permissions:
  contents: read

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r requirements.txt
      - run: python -m unittest discover -s tests -v
      - name: Run walk-forward spread and total backtests
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python scripts/backtest_spread_total.py
